# Manual PDFs

Place paywalled or otherwise inaccessible full-text PDFs here using the reference ID as the filename, for example `hardus_etal_2009.pdf`.

The audit script expects manually supplied files at `papers/manual/<reference_id>.pdf`.
